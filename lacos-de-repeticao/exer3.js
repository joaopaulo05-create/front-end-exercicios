# Solicita ao usuário que insira o número máximo
n = int(input("Digite o número máximo: "))

# Inicializa o contador
contador = 1

# Loop while para exibir os números de 1 até n
while contador <= n:
    print(contador)
    contador += 1
    